<%= form_for @job, :html => {:multipart => true} do |f| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% @job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Company Name: <%= f.text_field :company %>
  </div>
  <div class="field">
    Company Logo: <br>
    <%= f.file_field :companylogo %>
  </div>
  <div class="field">
    Job Title: <%= f.text_field :title %>
  </div>
  <div class="field">
    Job Description:<br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    Location:<br>
    <%= f.text_area :location %>
  </div>
  <div class="field">
Send your resumé to: <%= f.text_field :rhemail %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
